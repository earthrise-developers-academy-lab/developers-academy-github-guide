# Participant practice

Each participant receives an issue requesting a file at:

```text
docs/participants/<github-username>.md
```

The exercise demonstrates:

```text
Issue -> branch -> Markdown edit -> commit -> push -> pull request -> review -> merge
```

Include only information approved for publication.
